apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-yaci-store.fullname" . }}
  labels:
    {{- include "cf-yaci-store.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cf-yaci-store.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
