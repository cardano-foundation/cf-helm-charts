{{- $root := .Values -}}
{{ with index .Values "birth-cert-issuer" }}
{{- if .ingress.enabled -}}
{{- $birthCertIssuer := . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: birth-cert-issuer-ingress
  labels:
    app: birth-cert-issuer
  labels:
  {{- with .ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    {{- range $TLD := $root.ingressTLDs }}
      {{- range $host := $birthCertIssuer.ingress.hosts }}
    - host: {{ $host }}.{{ $TLD }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: birth-cert-issuer
                port:
                  number: {{ $birthCertIssuer.port | default 3457 }}
      {{- end }}
    {{- end }}
  tls:
    - hosts:
    {{- range $TLD := $root.ingressTLDs }}
      {{- range $host := $birthCertIssuer.ingress.hosts }}
        - "{{ $host }}.{{ $TLD }}"
      {{- end }}
    {{- end }}
{{ end }}
{{ end }}
