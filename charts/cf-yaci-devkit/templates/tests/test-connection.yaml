apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-yaci-devkit.fullname" . }}-test-connection"
  labels:
    {{- include "cf-yaci-devkit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-yaci-devkit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
